<div id="current-prd">
    <h1>Current Products</h1>
    <table>
        <tr>
            <th>Product</th>
            <th>Price</th>
            <th>Stock</th>
            <th>Action</th>
        </tr>
        <% if (products && products.length > 0) { %>
            <% products.forEach(product => { %>
                <tr>
                    <td><%= product.name %></td>
                    <td><%= product.price %></td>
                    <td><%= product.stockQuantity %></td>
                    <td>
                        <a href="/vendor/edit/<%= product._id %>">Edit</a>
                        <form method="POST" action="/users/vendor/delete/<%= product._id %>">
                            <input type="submit" value="Delete">    
                        </form>
                        
                    </td>
                </tr>
            <% }) %>
        <% } else { %>
            <tr>
                <td colspan="4">No products available.</td>
            </tr>
        <% } %>
    </table>
</div>

<!-- ... rest of the file ... -->

<div id="add-prd">
    <h1>Add Product</h1>
    <form method="post" action="/users/vendor/newproduct" enctype="multipart/form-data"> 
        <label for="fname">Product name:</label>
        <input type="text" id="fname" name="name" required>
        <label for="stock">In Stock:</label>
        <input type="number" id="stock" name="stockQuantity" min="0">
        <label for="price">Price:</label>
        <input type="number" id="price" name="price" required>
        <label for="description">Description:</label>
        <input type="text" id="description" name="description">
        <label for="product-image">Product Image:</label>
        <input type="file" id="product-image" name="productImage">
        <input type="submit" value="Submit">
    </form>
</div>
